# OpenAI
OPENAI_API_KEY=sk-...

# GitHub (personal access token with repo:status or public repo scope)
GITHUB_TOKEN=ghp_...

# ChromaDB HTTP endpoint (default to local Chroma server)
CHROMA_ENDPOINT=http://localhost:8000

# Vector collection name
CHROMA_COLLECTION=issue-labels

# Embedding model
EMBEDDING_MODEL=text-embedding-3-large

# LLM model (chat/completions)
LLM_MODEL=gpt-4o-mini
